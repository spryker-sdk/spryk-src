/**
 * @param \Spryker\Zed\Kernel\Container $container
 *
 * @return \Spryker\Zed\Kernel\Container
 */
public function provide{{ layer }}LayerDependencies(\Spryker\Zed\Kernel\Container $container): \Spryker\Zed\Kernel\Container
{
    $container = parent::provide{{ layer }}LayerDependencies($container);

    return $container;
}

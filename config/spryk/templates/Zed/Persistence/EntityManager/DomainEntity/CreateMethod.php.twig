${{ domainEntity | lcfirst }}Entity = $this->getFactory()
    ->create{{ domainEntity }}Mapper()
    ->map{{ domainEntity }}TransferTo{{ domainEntity }}Entity(${{ domainEntity | lcfirst }}Transfer, (new \Orm\Zed\{{ module }}\Persistence\Spy{{ domainEntity }}()));

${{ domainEntity | lcfirst }}Entity->save();

return $this->getFactory()
    ->create{{ domainEntity }}Mapper()
    ->map{{ domainEntity }}EntityTo{{ domainEntity }}Transfer(${{ domainEntity | lcfirst }}Entity, ${{ domainEntity | lcfirst }}Transfer);


/**
 * @param \Spryker\{{ application }}\Kernel\Container $container
 *
 * @return \Spryker\{{ application }}\Kernel\Container
 */
public function {{ provideMethod }}(\Spryker\{{ application }}\Kernel\Container $container)
{
    $container = parent::{{ provideMethod }}($container);

    return $container;
}

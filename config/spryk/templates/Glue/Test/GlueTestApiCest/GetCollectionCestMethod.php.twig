// Arrange
$I->addJsonApiResourcePlugin(new \{{ organization }}\Glue\{{ module }}\Plugin\GlueApplication\{{ resource | resourceNameToModelName }}Resource());

$url = $I->build{{ resource | resourceNameToModelName }}Url();

// Act
$I->sendGet($url, $I->build{{ httpMethod | ucfirst }}Collection{{ zedDomainEntity }}RequestParameters());

// Assert
$I->seeResponseCodeIs({{ httpResponseCode | httpResponseCodeToConstantName }});
$I->seeResponseJsonContains{{ zedDomainEntity }}Collection();
